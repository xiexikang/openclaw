# 恢复方案 C (手动确认模式) 并修复验证问题

用户反馈自动执行（方案 B）无效，且希望恢复到手动确认流程（方案 C）。
经分析，之前手动重发失败的原因可能是我引入的复杂 `userId` 提取逻辑导致 ID 不匹配。

## 计划步骤

1.  **恢复 `src/providers/qr-code.ts` 文案**
    - 将成功提示改回："扫码认证成功！请回到聊天窗口，重新发送之前的命令（或回复'确认'）即可执行。"
    - 移除 "系统将自动执行" 的误导性提示。

2.  **回退 `index.ts` 中的 `userId` 逻辑**
    - 移除我之前添加的 `sessionKey` 解析和 ID 组合逻辑。
    - 恢复使用最原始、最直接的 `const userId = ctx.sessionKey || "unknown";`。
    - 这能确保生成 Session 和检查验证时使用的是完全相同的字符串，避免解析错误导致的不匹配。

3.  **保留 Web 通道的提示**
    - 在 `notifyCallback` 中，保留通过 `deliverOutboundPayloads` 发送 "✅ 二次认证成功！..." 的提示，明确告知用户需要手动重发。

4.  **验证**
    - 重启服务。
    - 流程预期：
      1. 用户发送命令 -> 拦截。
      2. 用户扫码 -> 成功。
      3. 页面提示 "请回到聊天窗口..."。
      4. 聊天窗口收到 "✅ 二次认证成功..."。
      5. 用户重发命令（或回复"确认"） -> **执行成功**（因为 `userId` 匹配）。

这个方案回归了最简单可靠的逻辑，解决了自动执行无效的问题，并修复了因 ID 解析错误导致的验证死循环。
