## 修改计划

### 1. 添加环境变量支持到 config.ts

- `requireAuthOnFirstMessage` → `MFA_REQUIRE_AUTH_ON_FIRST_MESSAGE`（布尔值）
- `firstMessageAuthDuration` → `MFA_FIRST_MESSAGE_AUTH_DURATION`（毫秒数）

### 2. 更新 .env.example

- 添加这两个新环境变量的配置说明
- 提供配置示例
