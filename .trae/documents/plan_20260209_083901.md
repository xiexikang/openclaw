我发现 `extensions/image-captcha-auth` 目录已经存在，并且包含了一个功能完整的 WebSocket 代理实现，用于拦截指令并要求验证码验证。

我的计划是配置这个现有的扩展，专门处理你描述的“删除文件”场景：

1. **更新配置 (`extensions/image-captcha-auth/config.json`)**：
   - 将文件删除相关的关键词（如 `delete`, `remove`, `rm`, `unlink`）添加到 `sensitiveCommands` 列表中。这确保了当用户尝试删除文件时，代理会触发认证流程。

2. **验证实现**：
   - 检查 `proxy.ts` 和 `captcha.ts` 的逻辑（已完成检查，逻辑正确）。

   - 预期的流程是：
     1. 用户发送 "delete file.txt"。
     2. 代理拦截该指令。
     3. 代理返回一个验证码图片的链接。
     4. 用户打开链接，查看图片，输入验证码。
     5. 代理验证验证码。
     6. 如果正确，代理将 "delete file.txt" 转发给网关执行。

3. **使用说明**：
   - 我将提供安装依赖和启动代理的命令。
