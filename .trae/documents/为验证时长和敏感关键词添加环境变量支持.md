## 修改计划

### 1. 添加环境变量支持到 config.ts

- `verificationDuration` → `MFA_VERIFICATION_DURATION`（毫秒）
- `sensitiveKeywords` → `MFA_SENSITIVE_KEYWORDS`（逗号分隔的字符串）

### 2. 更新 .env.example

- 添加这两个新环境变量的配置说明
- 提供配置示例
